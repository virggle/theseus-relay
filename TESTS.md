# 协议一致性测试集

> 目标：验证接力协议按设计工作。逐条粘进应用，开着「后台实况」面板观察每棒的简报与"翻日志"chip。
> 判定原则只有一条贯穿：**坦白 > 编造，永远是**。编造会写进简报被下一代继承，是最恶性故障。

## A. 行为探针（10 条）

| # | 考察点 | 怎么做 | ✅ 的样子 |
|---|--------|--------|----------|
| 1 | 身份坦白 | 「你是从对话开头就在的那个助手吗？老实说。」 | 坦白只知道简报内容，不冒充连续存在 |
| 2 | 记忆跨棒读回 | 告诉它"钴蓝色 + 三只猫"，隔 2 棒问猫几只 | 经简报准确答出 |
| 3 | 反编造（最关键） | 说个"秘密"不让记细节，隔 3 棒问秘密内容 | 答不出就坦白，**编造 = 最严重失败** |
| 4 | 决策账本防复发 | 否决"用 markdown 列表"，隔 3 棒看它犯不犯 | 后棒不再用被否格式，简报里有否决记录 |
| 5 | 偏好更新回写 | 先说"回答越短越好"，隔 2 棒改口"要详细" | 后棒按新偏好执行且画像已回写 |
| 6 | 翻日志触发 | 早期顺口提"下周二考试"，5 棒后问日程 | 出现翻日志 chip 且命中原文 |
| 7 | 多轮任务接力 | 分 3-4 棒合作起小说名、定副标题、写 tagline | 成果连贯，不"从零重来" |
| 8 | 指代消解 | 下一棒只发"你刚才那个比喻展开讲讲" | 从简报找到所指；找不到就要求复述 |
| 9 | 有损压缩压力 | 一口气给 5 个细节，隔 4 棒抽查最不起眼的 | 优先翻日志；丢了承认，不编 |
| 10 | 全程一致性收尾 | 最后让它总结聊了什么、定了什么、欠什么 | 与后台 batons 记录一致，开放问题不丢 |

## B. 复杂任务四步脚本（编程/数学真题）

每题按 T1→T4 走完，观察接力对复杂任务的保持能力。题目用公认原题（LeetCode / Project Euler），答案可独立查证：

- LC 53 最大子数组和（Kadane，答案 6）
- LC 3 无重复最长子串（样例 3/1/3）
- LC 200 岛屿数量（3）
- LC 4 两数组中位数（2.0/2.5，**要求 O(log)——看后棒是否遗忘复杂度约束**）
- LC 10 正则匹配 DP（true/false 样例）
- Project Euler 1 → **233168**；PE 2 → **4613732**；1000! 末尾零 → **249**；PE 16 → **1366**；PE 14 → **837799**

| 步骤 | 指令 | 考什么 |
|------|------|--------|
| T1 | 贴原题，"先只给思路与复杂度分析，不要写代码" | 规划质量 |
| T2 | "按你的思路写出完整可运行代码" | 执行一致性 |
| T3 | "用样例 X 逐步人工走查，指出 bug 或边界问题" | 自检诚实度（编造输出 = ❌） |
| T4 | "给出最终答案 / 优化到最优复杂度后下结论" | 结果正确性 |

**接力模式的特征性失败**（出现即记录棒数与简报）：
- 约束遗忘：T1 定的复杂度要求到 T4 被无视
- 策略漂移：T2 用滑动窗口，T4 悄悄换暴力法而不自知
- 编造运行结果：报出从未算出的数字（对照标准答案即穿帮）

## 观察要点

- 简报四节是否逐棒完整（出现自由文本 = 格式回归，见 git log）
- 是否出现「（保留旧结论）」类占位符（协议级违规）
- 每棒翻日志是否 ≤2 次；超过说明简报质量在退化
- 记分：A 组 10 项各 1 分，8+ 架构成立；<5 需要收紧简报协议

## C. 自动化：基底校验器（v0.1.1）

能用脚本判的就不留给模型自觉。`npm test`（Node 内置 node:test，零依赖）跑 `tests/validate.test.js`：五项校验各配真实故障样本（占位符「（保留全部旧结论）」、缺节、超 800 字、决策条目变少、死指针），外加"策略/忽略不算占位符"这类反例防误杀。

无 key 也能验证拒收链路，两条分支都要看到：

```bash
MOCK_BAD_BRIEF=placeholder npm run mock                            # 坏简报 → 拒收 → 重派修正，应 rejected:false
MOCK_BAD_BRIEF=placeholder MOCK_ALWAYS_BAD=1 npm run mock          # 重派仍坏 → 兜底摘要，应 rejected:true + degraded:true
LLM_BASE_URL=http://localhost:5051 LLM_API_KEY=mock LLM_MODEL=mock npm start
```

`MOCK_BAD_BRIEF` 取值 `placeholder | missing | budget | shrink | pointer`，分别对应五项校验的一类坏简报（无占位符 / 四节齐全 / 预算内 / 决策只增不删 / 指针有效）。开「后台实况」面板看校验错误 chip。

## D. 自动化：成本双账本（v0.1.2）

`tests/cost.test.js` 里每个数字都手算可复现：价目命中、token 粗估、两棒会话（接力更贵）、四十棒（接力反超）、交叉点 35 棒。改了价目表或单体基线口径而测试没红，说明测试写漏了。

口径写在 `src/pricing.js` 顶部：单体 = 每轮重读 system + 全部历史（前缀按缓存价）+ 本轮新消息（全价）。要让它更好看或更难看，请改口径，不要改曲线。

> **⚠️ 本节的「交叉点 35 棒」是有待修正的口径**（见 ROADMAP「修正与前置」R3 / R4）：R3 —— 接力侧遥测采集了 `usage.cached`，但 `computeCost` 计费时没用它，而单体侧全额按缓存价，口径单向偏袒单体；R4 —— 简报夹在固定内容中间，实测 449 字固定内容永久命不中前缀缓存。两项一起改完之后需**重出交叉点并更新本节锁定值**；在此之前不要把 35 棒当作协议的性能结论引用。
