# 接力协议 · Relay Protocol v0.1

> 忒修斯号的核心不是聊天界面，而是这份协议：**无状态工人 + 有状态协议**。任何模型、任何 runner，只要实现本协议即可接入接力系统。本文档是"从 chatbox 进化为 harness"的那条线头。

## 1. 参与者

| 角色 | 生命周期 | 看得见什么 |
|------|---------|-----------|
| **棒（Batons）** | 一轮用户消息即一生，回答完即销毁 | ① 上一棒的《简报》 ② 用户本轮消息 ③（受限）检索到的历史片段 |
| **基底（Substrate）** | 永生 | 全部历史 log、历代简报、检索索引 |
| **用户** | 上帝视角 | 完整 log + 后台面板（每棒输入/输出/检索行为全程可审计） |

**核心不变量：棒与棒之间唯一的传输介质是《简报》。** 简报丢失的信息就是真正丢失的信息——系统的所有工程努力都服务于让"该活的活下来，该死的痛快死"。

## 2. 回合契约（Turn Contract）

一个回合必须按序完成三件事，缺一不可：

```
1. READ   读入简报 + 用户消息（此为该棒的全部世界）
2. ACT    回答用户（可先执行一次受限检索：{"action":"read_log","query":"…"}）
3. WRITE  输出更新后的简报（累积压缩，非本轮纪要）
```

传输格式为单个 JSON：`{"reply": "...", "handoff": "..."}`。模型输出损坏时由基底负责抢救（见 §5），但对模型的合同要求始终是一次输出完整。

## 3. 简报模式（Brief Schema）

四节固定结构，任何一代都必须完整输出：

```
## 进展        此前脉络的压缩 + 本代新进展
## 决策        已确定结论、被否方案及理由 —— 只增不删
## 用户画像    沟通风格、背景、在意什么 —— 合并更新
## 开放问题    已解决的清掉，新出现的加上
```

### 三条硬性不变量

1. **累积压缩**：简报 = 上一份简报中仍相关内容的压缩保留 + 本代新增。它永远不是"本轮纪要"。
2. **禁止元注释**：「（保留全部旧结论）」「同上」「略」是协议级违规——基底看不到旧简报原文，占位符等于销毁信息。决策与画像必须逐条完整写出，允许与上一份一字不差。
3. **有损预算**：总长硬上限（当前 800 字），丢弃优先级：`进展细节 < 开放问题 < 用户画像 < 决策与约束`。**决策是最神圣的一节**——它代表协议的免疫系统（防止后棒重提已否方案）。

## 4. 外部记忆检索（Log-as-Memory）

完整 log 永不自动进入任何棒的上下文。棒可通过翻日志动作按需检索：

- 每棒最多 2 次，每次最多 8 条命中（朴素 n-gram 打分即可，无需向量库）
- 检索结果以"系统注入"形式给出，并明确告知：*这只是片段，完整记录你依然看不到*
- 语义：**log 是档案库，不是工作记忆**。检索是探照灯，不是开灯

## 5. 故障语义（Failure Semantics）

| 故障 | 基底行为 |
|------|---------|
| JSON 损坏但 reply 字段可救 | 抢救正文，标记 `salvaged`，简报走独立摘要调用 |
| JSON 完全不可解析 | 整段输出视为 reply，简报走独立摘要调用 |
| reply + 简报超长截断 | maxTokens 上限需覆盖 reply+简报之和；截断由抢救器兜底 |
| 模型拖延（"稍后给你"） | 提示词级禁止：reply 与简报必须当轮完成 |
| 棒整体失败 | 该棒从未存在——简报仍指向上一个成功棒，**棒级事务性天然成立** |

## 6. 为什么这是 Harness 的种子

当前实现是"对话接力"，但协议本身已经包含了 harness 的全部要素，只差三个扩展点：

1. **工具棒（v0.2）**：ACT 阶段允许调用工具，且**副作用账本**必须写入简报（"本棒执行过哪些不可逆操作"）——无状态工人也能安全干活的前提
2. **基底化（v0.3）**：简报从 prose 降级为**指针**（状态文件路径、任务列表 ID），真状态落盘。简报越薄，系统越稳
3. **审查棒（v0.4）**：执行棒与审查棒交替。审查棒的审查是真无偏见的——它结构性地没看过写作过程，这是单 Agent 架构做不到的

更远的推论：简报 schema 版本化之后，**棒可以跨模型、跨厂商接棒**（A 棒用便宜模型跑杂务，关键棒换强模型）——成本调度变成协议层的事。

## 7. 已知边界（诚实清单）

- 简报压缩方差大：不同棒对"什么重要"判断不同，长程信息衰减不可避免（这是特性，也是 bug）
- 中文 2-gram 检索是低保真方案，够 demo 但不够生产
- 每棒 2-3 次 LLM 调用（答+抢救兜底），成本高于单 Agent 连续对话——买的是有界上下文与审计性
- 翻日志的"每棒 2 次"限额曾被模型误解为需用户批准——工具语义的提示词要写死
